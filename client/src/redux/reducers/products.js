const initialState = {
  product: "product",
};

export const products = (state = initialState, { type, payload }) => {
  switch (type) {
    /* case value:
      return; */
    default:
      return state;
  }
};
